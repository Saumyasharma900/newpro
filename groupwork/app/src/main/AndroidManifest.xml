<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group_work">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Groupwork">
        <activity android:name=".Pet_activity"></activity>
        <activity
            android:name=".HI"
            android:label="@string/title_activity_hi"></activity>
        <activity
            android:name=".Newac"
            android:label="@string/title_activity_newac"></activity>
        <activity
            android:name=".Pet2Activity2"
            android:label="@string/title_activity_pet22"></activity>
        <activity
            android:name=".Loginpage"
            android:parentActivityName=".Firstpage" />
        <activity
            android:name=".Homepage"
            android:label="@string/title_activity_homepage" />
        <activity
            android:name=".Signup"
            android:parentActivityName=".Firstpage" />
        <activity android:name=".Firstpage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>